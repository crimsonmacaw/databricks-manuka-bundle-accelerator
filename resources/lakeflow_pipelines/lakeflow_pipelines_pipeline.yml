resources:
  pipelines:
    lakeflow_pipelines_python_pipeline:
      name: lakeflow_pipelines_python_pipeline
      serverless: true
      channel: "PREVIEW"
      catalog: ${var.catalog}
      schema: ${var.schema}
      root_path: "../src/lakeflow_pipelines"
      libraries:
        - glob:
            include: transformations/**
